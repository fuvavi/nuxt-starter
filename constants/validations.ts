export const REGEX = {
    USERNAME: /^[a-z0-9]+$/ // Include aphabet and number,
}

export const MIN_LENGTH = {
    USERNAME: 6,
    PASSWORD: 6,
    EMAIL: 4
}

export const MAX_LENGTH = {
    USERNAME: 30,
    PASSWORD: 32,
    EMAIL: 50
}
